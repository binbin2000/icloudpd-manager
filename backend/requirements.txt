fastapi==0.111.0
uvicorn[standard]==0.29.0
apscheduler==3.10.4
aiosqlite==0.20.0
aiofiles==23.2.1
python-multipart==0.0.9
pydantic==2.7.1
croniter==2.0.5
icloudpd==1.32.2
# Runtime deps for pyicloud_ipd (the library bundled inside icloudpd's binary).
# pyicloud_ipd source is extracted from the icloudpd git repo via Dockerfile and
# placed on PYTHONPATH; these are the Python packages it needs at runtime.
requests
srp
keyring
keyrings-alt
